// The structured conversation view is hidden, not removed: the workbench now hosts a real CLI terminal.
// Flip this to true to bring the chat, lp-edit review flow and context checkboxes back.
export const chatView = false;
